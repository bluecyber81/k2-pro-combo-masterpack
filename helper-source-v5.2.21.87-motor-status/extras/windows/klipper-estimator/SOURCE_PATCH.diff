diff --git a/lib/src/slicer.rs b/lib/src/slicer.rs
index 0aafe73..ecb8511 100644
--- a/lib/src/slicer.rs
+++ b/lib/src/slicer.rs
@@ -5,6 +5,8 @@ pub enum SlicerPreset {
     PrusaSlicer { version: String },
     SuperSlicer { version: String },
     OrcaSlicer { version: String },
+    CrealityPrint { version: String },
+    CrealityCloudSlicer { version: String },
     IdeaMaker { version: String },
     Cura { version: Option<String> },
     Simplify3D { version: String },
@@ -16,6 +18,12 @@ impl std::fmt::Display for SlicerPreset {
             SlicerPreset::PrusaSlicer { version } => write!(f, "PrusaSlicer {}", version),
             SlicerPreset::SuperSlicer { version } => write!(f, "SuperSlicer {}", version),
             SlicerPreset::OrcaSlicer { version } => write!(f, "OrcaSlicer {}", version),
+            SlicerPreset::CrealityPrint { version } => {
+                write!(f, "Creality Print {}", version)
+            }
+            SlicerPreset::CrealityCloudSlicer { version } => {
+                write!(f, "Creality Cloud Slicer {}", version)
+            }
             SlicerPreset::IdeaMaker { version } => write!(f, "ideaMaker {}", version),
             SlicerPreset::Cura { version: None } => write!(f, "Cura"),
             SlicerPreset::Cura {
@@ -28,13 +36,36 @@ impl std::fmt::Display for SlicerPreset {
 
 impl SlicerPreset {
     pub fn determine(comment: &str) -> Option<SlicerPreset> {
-        None.or_else(|| Self::try_slic3r(comment))
+        None.or_else(|| Self::try_creality_print(comment))
+            .or_else(|| Self::try_creality_cloud_slicer(comment))
+            .or_else(|| Self::try_slic3r(comment))
             .or_else(|| Self::try_ideamaker(comment))
             .or_else(|| Self::try_cura_old(comment))
             .or_else(|| Self::try_cura_new(comment))
             .or_else(|| Self::try_simplify3d(comment))
     }
 
+    fn try_creality_print(comment: &str) -> Option<SlicerPreset> {
+        lazy_static! {
+            static ref RE: Regex =
+                Regex::new(r"generated by Creality_Print V([^\s]+) on").unwrap();
+        }
+        RE.captures(comment).map(|c| SlicerPreset::CrealityPrint {
+            version: c.get(1).unwrap().as_str().into(),
+        })
+    }
+
+    fn try_creality_cloud_slicer(comment: &str) -> Option<SlicerPreset> {
+        lazy_static! {
+            static ref RE: Regex =
+                Regex::new(r"generated by Creality_Cloud_Slicer V([^\s]+) on").unwrap();
+        }
+        RE.captures(comment)
+            .map(|c| SlicerPreset::CrealityCloudSlicer {
+                version: c.get(1).unwrap().as_str().into(),
+            })
+    }
+
     #[allow(clippy::manual_map)]
     fn try_slic3r(comment: &str) -> Option<SlicerPreset> {
         lazy_static! {
@@ -94,3 +125,36 @@ impl SlicerPreset {
         })
     }
 }
+
+#[cfg(test)]
+mod tests {
+    use super::SlicerPreset;
+
+    #[test]
+    fn detects_creality_print_header() {
+        let preset = SlicerPreset::determine(
+            " generated by Creality_Print V7.2.0.5226 on 2026-07-11 at 09:30:25",
+        )
+        .expect("Creality Print header should be detected");
+
+        assert!(matches!(
+            preset,
+            SlicerPreset::CrealityPrint { ref version } if version == "7.2.0.5226"
+        ));
+        assert_eq!(preset.to_string(), "Creality Print 7.2.0.5226");
+    }
+
+    #[test]
+    fn detects_creality_cloud_slicer_header() {
+        let preset = SlicerPreset::determine(
+            " generated by Creality_Cloud_Slicer V7.1.0.0 on 2026-05-20 at 00:18:15",
+        )
+        .expect("Creality Cloud Slicer header should be detected");
+
+        assert!(matches!(
+            preset,
+            SlicerPreset::CrealityCloudSlicer { ref version } if version == "7.1.0.0"
+        ));
+        assert_eq!(preset.to_string(), "Creality Cloud Slicer 7.1.0.0");
+    }
+}
diff --git a/tool/src/cmd/post_process.rs b/tool/src/cmd/post_process.rs
index 341ca66..d249527 100644
--- a/tool/src/cmd/post_process.rs
+++ b/tool/src/cmd/post_process.rs
@@ -304,6 +304,8 @@ fn metadata_processor(preset: &SlicerPreset) -> Box<dyn GCodeInterceptor> {
         SlicerPreset::PrusaSlicer { .. } => Box::<PSSSGCodeInterceptor>::default(),
         SlicerPreset::SuperSlicer { .. } => Box::<PSSSGCodeInterceptor>::default(),
         SlicerPreset::OrcaSlicer { .. } => Box::<PSSSGCodeInterceptor>::default(),
+        SlicerPreset::CrealityPrint { .. } => Box::<PSSSGCodeInterceptor>::default(),
+        SlicerPreset::CrealityCloudSlicer { .. } => Box::<PSSSGCodeInterceptor>::default(),
         SlicerPreset::IdeaMaker { .. } => Box::<IdeaMakerGCodeInterceptor>::default(),
         SlicerPreset::Cura { .. } => Box::<CuraGCodeInterceptor>::default(),
         SlicerPreset::Simplify3D { .. } => Box::<Simplify3DGCodeInterceptor>::default(),
