[Service]
# Local Unix socket only. This enables diagnostics and screenshots without
# exposing the HelixScreen control API on the LAN.
ExecStart=
ExecStart=/home/mks/helixscreen/bin/helix-launcher.sh --remote --remote-socket /run/helixscreen/helixctl.sock
